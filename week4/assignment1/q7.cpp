#include <iostream>
using namespace std;
int main()
{
    cout << "In C++, functions with the same name can have different parameter lists, but the return type must be the same. This is known as function overloading. Function overloading allows you to define multiple functions with the same name, but with different parameter lists, so that you can use the same function name to perform different task";
}
